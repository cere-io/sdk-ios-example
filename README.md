# Test application for iOS SDK integration 

### Quick start:
1. Install CocoaPod via brew or get (for M1 silicon - better to use brew):
```bash
brew install cocoapods
```
2. Install pods:
```bash
pod deintegrate
pod clean
pod setup
pod install
```
3. Start XCode and build the project